---
created: 2026-09-24T08:44:39.833Z
session: cc5d5d95-66be-455c-b806-4aeab321e0bc
working_directory: e:\DevStuff\snowflake-forge
---

### Implementation Plan: Task B01 — Database, Schemas & Warehouse Setup

#### 1. Context & Objectives
Task B01 sets up the core foundation for Supply Chain Forge in Snowflake according to `docs/CONTRACT.md` and `.agents/tasks/coco/B01_database.md`:
- Database: `SUPPLY_CHAIN_FORGE`
- 7 Dedicated Schemas representing the 4 source systems, governed layer, semantic layer, and app layer:
  - `ERP_SOURCE` (Enterprise Resource Planning: customers, orders, order lines)
  - `WMS_SOURCE` (Warehouse Management System: plants, inventory snapshots)
  - `TMS_SOURCE` (Transport Management System: shipments, carriers)
  - `SRM_SOURCE` (Supplier Relationship Management: suppliers, parts, sourcing)
  - `GOVERNED` (Conformed views, masking policies, persona stored procedures)
  - `SEMANTIC` (Semantic view ontology, Cortex Agent, MCP server)
  - `APP` (Streamlit application schema)
- Drop default `PUBLIC` schema if present
- Warehouse: `FORGE_WH` (`XSMALL`, `AUTO_SUSPEND = 60`, `AUTO_RESUME = TRUE`, `INITIALLY_SUSPENDED = TRUE`)

---

#### 2. Concrete Implementation Steps

1. **Populate SQL Script (`sql/01_setup/01_database.sql`)**:
   - Write complete DDL statements with descriptive comments for database, all 7 schemas, `PUBLIC` schema cleanup, and `FORGE_WH` warehouse configuration.

2. **Execute DDL via Snowflake (`sql_execute`)**:
   - Run the script against the active Snowflake connection (`tyduokn-gf25237`).

3. **Verify Gate Criteria**:
   - `SHOW SCHEMAS IN DATABASE SUPPLY_CHAIN_FORGE;` → Confirm exactly 7 schemas + `INFORMATION_SCHEMA`.
   - `SHOW WAREHOUSES LIKE 'FORGE_WH';` → Verify size `XSMALL`, auto-suspend 60s, auto-resume `TRUE`.
   - `USE DATABASE SUPPLY_CHAIN_FORGE; USE WAREHOUSE FORGE_WH; SELECT CURRENT_DATABASE(), CURRENT_WAREHOUSE();` → Confirm context switches cleanly.

4. **Update Agent Tracking & Handoff Logs**:
   - `.agents/NEXT.md` → Mark B01 as completed (✅) and set B02 as NEXT.
   - `.agents/HANDOFF.md` → Update deployed object inventory (Database, 7 schemas, Warehouse).
   - `.agents/tasks/COCO_TASKS.md` → Mark B1 tasks as checked.
   - `docs/SESSION_LOG.md` → Log session execution, credit usage, and next step.

---

#### 3. Verification & Safety Checks
- Verify `AUTO_SUSPEND = 60` and `INITIALLY_SUSPENDED = TRUE` to ensure zero idle credit consumption.
- Ensure strict adherence to frozen naming conventions in `docs/CONTRACT.md`.