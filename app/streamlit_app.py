# Supply Chain Forge — Streamlit Demo App
# Owner: Claude Code
# Deployed to: Snowflake (Streamlit in Snowflake) or run locally

# TODO: Claude Code will build this
# Requirements:
#   1. Persona selector dropdown (Planner, Buyer, Logistics Manager)
#   2. Natural language query input
#   3. Route queries through Cortex Agent
#   4. Display results with metric lineage
#   5. Consistency proof tab: same query, 3 personas, identical results
#   6. Metric visualizations (charts)

# Placeholder — Claude Code will replace this entirely
import streamlit as st

st.set_page_config(page_title="Supply Chain Forge", layout="wide")
st.title("Supply Chain Forge")
st.info("This app is under construction. Claude Code will build it once CoCo deploys the Snowflake objects.")
